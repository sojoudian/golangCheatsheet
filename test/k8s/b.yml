apiVersion: v1
kind: Pod
metadata:
  name: second-ginx
  labels:
    #env: production
    app: myapp

spec:
  containers:
    - name: nginx
      image: nginx
    # - name: busybox
    #   image: busybox
